// -----------------------------------------------------------------------------
// Utils (v2.0)
//
// Simple functions, macros, structures, ... that were, are or could be useful
// -----------------------------------------------------------------------------

using namespace std;


#ifndef _utils
#define _utils

#define max(a,b)    (a > b) ? a : b
#define min(a,b)    (a < b) ? a : b
#define abs(a)      (a > 0) ? a : -a

#define debug       1

enum class ProblemType { MIN, MAX };

#endif
